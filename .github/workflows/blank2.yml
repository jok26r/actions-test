name: Test
on:
    workflow_dispatch:
jobs:
    dummy:
        name: dummy
        runs-on: ubuntu-latest
        steps:
        -   name: Print input
            run: echo "${{github.event.inputs.in1}}"
